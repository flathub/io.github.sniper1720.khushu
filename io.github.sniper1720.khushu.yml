app-id: io.github.sniper1720.khushu
runtime: org.gnome.Platform
runtime-version: "50"
sdk: org.gnome.Sdk
sdk-extensions:
  - org.freedesktop.Sdk.Extension.rust-stable
separate-locales: false
command: khushu
finish-args:
  - --share=network
  - --share=ipc
  - --socket=fallback-x11
  - --socket=wayland
  - --socket=pulseaudio
  - --device=dri
  - --talk-name=org.kde.StatusNotifierWatcher
  - --env=LOCPATH=/app/usr/lib/locale
modules:
  - name: khushu
    buildsystem: meson
    build-options:
      append-path: /usr/lib/sdk/rust-stable/bin
      env:
        CARGO_HOME: /run/build/khushu/cargo
        CARGO_NET_OFFLINE: "true"
        CARGO_BUILD_OFFLINE: "true"
        GETTEXT_SYSTEM: "1"
      config-opts:
        - -Dgenerate_locales=auto
    sources:
      - type: archive
        url: https://github.com/sniper1720/khushu/archive/refs/tags/v1.3.2.tar.gz
        sha256: 865152805e3710ba3a81664c7d369beff12d94b0d8b3bae0220cd45dde010615
      - cargo-sources.json
